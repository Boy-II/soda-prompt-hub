# 更新记录

本文件记录 Soda Prompt Hub 面向使用者的重要变化。开发过程和历史验收细节继续保存在
`task_plan.md`、`progress.md` 与 `findings.md`。

## 未发布（1.1.0rc1 候选版）

### 新增

- 建立程序版本、发布通道、数据结构版本与 Windows Worker 协议的统一版本信息。
- 首页显示当前程序和数据结构版本，设备诊断显示 Worker 版本及兼容状态。
- 增加 Mac 安全更新器，更新前备份个人资料和旧程序，初始化失败时自动恢复。
- 增加可校验的 Windows Worker 独立 ZIP、首次配置入口和升级顺序。
- 将公开文档分为快速开始、核心工作流、Mac、Windows、设备关系、排错和发布体系。

### 破坏性变更

- 外部模型配置升级为“端点 + 勾选模型”结构，旧 `/api/model-connections*` 路径已移除；
  请使用 `/api/model-endpoints*` 管理多组 OpenAI-compatible 端点。

## [1.0.0] - 2026-09-05

### 主要能力

- 完成从灵感、OC 和视觉参考到 Anima/Krea 2 双格式 Prompt 的创作流程。
- 接入本地提示词来源、网页来源卡、CLIP 以图找图与视觉参照索引。
- 完成 5060 Ti ComfyUI 远程出图、结果回流、LoRA/底模只读清单和预览。
- 完成数据集扫描、WD14 草稿、人工审核、版本冻结、哈希校验和 Windows 交付。
- 增加 LM Studio 本地优先及可选 OpenAI-compatible 外部模型连接。
- 完成个人事实数据备份、隔离恢复、任务恢复与中文操作引导。

### 安全与边界

- API Key 仅保存在本机私有配置中，网页与接口不返回明文。
- 公共资料、OC Manager 和外部数据集默认只读，导出使用独立版本目录。
- Prompt Hub 不负责启动 LoRA 训练；训练、正则和最终标签筛选继续在 Windows 工具中完成。

### 发布说明

- 软件包版本统一为 `1.0.0`，产品名称统一为 Soda Prompt Hub。
- 增加公开仓库自动检查流程。
- 代码采用 MIT License；第三方提示词、图片、模型和数据集仍遵循各自来源条款。
