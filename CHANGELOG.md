# 更新记录

本文件记录 Soda Prompt Hub 面向使用者的重要变化。开发过程和历史验收细节只保存在维护者本机，
不进入公开仓库和发行包。

## [1.1.0rc2] - 2026-09-09

### 新增

- 数据集工作台增加通用、肖像、服装和风格四种说明文字模式，可按训练目标省略对应内容并添加触发词。
- Anima 草稿可按图片类型选择 WD SwinV2 v3 或 IdolSankaku Tagger，也可以使用已连接的视觉模型。
- Krea 2 草稿支持按需中文对照、根据中文意见改写，并在逐张检查时保留每张图片的译文。
- 说明文字设置可以在当前浏览器保存为预设，供后续数据集重复使用。
- 界面增加简体中文、繁体中文和英文切换，并记住本机选择。

### 修复

- 设备任务支持把已经处理过的 Windows 返回记录单条或批量移入历史，不会导入结果或删除返回文件。
- Krea 2 的 `ImpactWildcardEncode` 工作流可以从 `populated_text` 读取通配符展开后的完整 Prompt。
- Windows Worker ZIP 使用固定文件时间和权限；同一提交、同一工具链重复构建得到相同 SHA-256。
- 自动生成说明不再无条件追加 `photo, realistic`，而是按图片真实媒材决定是否保留媒材信息。
- 数据集来源目录消失时会立即说明原因，不再逐张失败到队列末尾。
- 模型接入不再因为编辑状态残留而覆盖另一组已保存服务。
- 局域网 IP 上的 OpenAI-compatible 模型服务可以使用 HTTP；公网服务仍要求 HTTPS。
- “打标模型”统一表示 WD14 或视觉模型的选择，避免与通用、肖像、服装和风格模式混淆。
- 补齐三语切换下的数据集状态、设备任务、模型目录与模型接入动态文案；模型名、LoRA 名和用户备注仍保留原文。
- 修复手机端设备页四个子页面名称互相挤压的问题，较长名称会在按钮内换行显示。
- 移除翻译表中误收录的个人目录与 Windows 主机名，改用不保存真实运行值的动态翻译规则。

### 仓库与发布

- 本机开发计划、调查记录和进度记录不再由 Git 跟踪，也不会进入公开仓库或发行包。
- 更新公开开发计划与 Windows Worker 可重复构建说明。

## [1.1.0rc1] - 2026-09-07

### 新增

- 建立程序版本、发布通道、数据结构版本与 Windows Worker 协议的统一版本信息。
- 首页显示当前程序和数据结构版本，设备诊断显示 Worker 版本及兼容状态。
- 增加 Mac 安全更新器，更新前备份个人资料和旧程序，初始化失败时自动恢复。
- 增加可校验的 Windows Worker 独立 ZIP、首次配置入口和升级顺序。
- 将公开文档分为快速开始、核心工作流、Mac、Windows、设备关系、排错和发布体系。

### 破坏性变更

- 外部模型配置升级为“端点 + 勾选模型”结构，旧 `/api/model-connections*` 路径已移除；
  请使用 `/api/model-endpoints*` 管理多组 OpenAI-compatible 端点。

## [1.0.0] - 2026-09-05

### 主要能力

- 完成从灵感、OC 和视觉参考到 Anima/Krea 2 双格式 Prompt 的创作流程。
- 接入本地提示词来源、网页来源卡、CLIP 以图找图与视觉参照索引。
- 完成 5060 Ti ComfyUI 远程出图、结果回流、LoRA/底模只读清单和预览。
- 完成数据集扫描、WD14 草稿、人工审核、版本冻结、哈希校验和 Windows 交付。
- 增加 LM Studio 本地优先及可选 OpenAI-compatible 外部模型连接。
- 完成个人事实数据备份、隔离恢复、任务恢复与中文操作引导。

### 安全与边界

- API Key 仅保存在本机私有配置中，网页与接口不返回明文。
- 公共资料、OC Manager 和外部数据集默认只读，导出使用独立版本目录。
- Prompt Hub 不负责启动 LoRA 训练；训练、正则和最终标签筛选继续在 Windows 工具中完成。

### 发布说明

- 软件包版本统一为 `1.0.0`，产品名称统一为 Soda Prompt Hub。
- 增加公开仓库自动检查流程。
- 代码采用 MIT License；第三方提示词、图片、模型和数据集仍遵循各自来源条款。
